import WalletButton from "../components/WalletButton"

export default function Home() {
  return (
    <div style={{
      height: "100vh",
      display: "flex",
      justifyContent: "center",
      alignItems: "center"
    }}>
      <WalletButton />
    </div>
  )
}
